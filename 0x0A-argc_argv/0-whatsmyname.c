#include <stdio.h>

/**
 * main - the main function.
 * @argc: conter of inputs.
 * @argv: the input.
 * Return: 0.
*/
int main (int argc, char *argv[])
{
	(void)argc;
	printf ("%s\n", argv[0]);
	return (0);
}
